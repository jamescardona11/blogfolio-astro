---
export const prerender = false // server

import { getCourses } from '@/api/resume/education'

const coursesItems = await getCourses()

console.log(coursesItems)
---

<div>
  <h1>Education</h1>

  <ul class='grid grid-cols-2 gap-2 space-y-1'>
    {
      coursesItems?.map(course => (
        <li class='flex items-center justify-between'>
          <div class='inline-flex mr-1 truncate grow'>
            <span class='mr-2 text-sky-500'>{'>'}</span>
            <a
              class='font-sans font-[650] text-sm truncate'
              href={course.link}
              target='_blank'
              rel='noreferrer'
            >
              {course.name}
            </a>
          </div>
        </li>
      ))
    }
  </ul>
</div>
 @/server/resume/education
