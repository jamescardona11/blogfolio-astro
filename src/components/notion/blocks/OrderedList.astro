---
import type { OrderedList } from '@/lib/data/notion-core/notion-blocks'
import NotionText from './NotionText.astro'

interface Props {
  block: OrderedList
}

const { block } = Astro.props
---

<ul class=''>
  {
    block.items.map((listItem, index) => {
      return (
        <li class='flex items-center space-x-2'>
          <span class='font-medium text-foreground'>{index}.</span>
          <p>
            <NotionText block={listItem.richText} />
          </p>
        </li>
      )
    })
  }
</ul>
